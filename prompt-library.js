/* Prompt Studio Clean V1 canonical library.
   Intentionally starts with zero prompts after the ground-up reset.
   New prompts must enter through the clean Prompt Studio workflow. */
window.FPL_PROMPT_LIBRARY = [];
